apiVersion: v1
kind: Service
metadata:
  name: {{ include "openhands-claude.fullname" . }}
  labels:
    {{- include "openhands-claude.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openhands-claude.selectorLabels" . | nindent 4 }}
